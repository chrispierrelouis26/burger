
<h1>Add Burger</h1>



<ul>
  {{#each burgers}}
  	<li>
  	   <p>ID: {{this.id}}</p>
       <p>Burger: {{this.burger_name}}</p>
  	</li>
  {{/each}}
</ul>

<div>
  <form action="/" method="POST">
  	<textarea type="text" name="wish"></textarea>

  	<button type="submit">Submit</button>
  </form>
</div>





<ul>
  {{#each burgers }}
  <li>
    <p>
      {{this.id}}. {{this.burger_name}}

      <button data-burger_nameid="{{this.id}}" class="delplan">Delete Plan!</button>
    </p>
  </li>
  {{/each}}
</ul>

<h2>Add ya a Burger!</h2>
<form action ="/" method="POST">
  <textarea type="text" name="burger"></textarea>
  <button type="submit">Save Plan!</button>
</form>

<h2>Update ya burger!</h2>
<form action ="/" method="POST">
  <select name="id">
		{{#each plans}}
			<option value="{{this.id}}">{{this.burger_name}}</option>
		//{{/each}}
	</select>
  <textarea type="text" name="plan" placeholder="plan"></textarea>
  <button type="submit">Update Burger</button>
</form>

{{!-- <script type="text/javascript">
  $(".delplan").on("click", function(event) {
     $(this).attr("data-planid")
    var id = $(this).data("planid");

    $.ajax("/todos/" + id, {
      type: "DELETE"
    }).then(
      function() {
        console.log("deleted id ", id);
        // Reload the page to get the updated list
        location.reload();
      }
    );
  });  --}}